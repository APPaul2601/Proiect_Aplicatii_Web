{
  "name": "castle-clicker-game",
  "version": "1.0.0",
  "description": "A web-based castle clicker game built with React and Node.js",
  "main": "backend/server.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run start --prefix frontend\"",
    "start": "npm start --prefix frontend",
    "build": "npm run build --prefix frontend",
    "build:backend": "npm install --prefix backend",
    "build:frontend": "npm install --prefix frontend && npm run build --prefix frontend"
  },
  "keywords": [
    "game",
    "clicker",
    "castle",
    "react"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
